#include<stdio.h>
int sum_of_digits(int n);
main()
{
	int num=1234;
	int sum=sum_of_digits(num);
	printf("input number: %d \n sum of digits: %d",num,sum);
	
	return 0;
}

int sum_of_digits(int n)
 {
	if(n==0)
	{
	return 0;
	}
	else
	{
		return (n%10 + sum_of_digits(n/10));
	}
	
}
